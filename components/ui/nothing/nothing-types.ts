/**
 * Nothing Design — shared types for primitives.
 * Token references: references/tokens.md, references/components.md
 */

export type NdTextColor =
  | "primary"
  | "secondary"
  | "disabled"
  | "display"
  | "accent"
  | "success"
  | "warning"
  | "info";

export type NdTone = "neutral" | "good" | "moderate" | "over" | "info";
