"use client";

/**
 * Maps page route wrapper.
 *
 * The actual Google Maps instance and tools are hosted globally in `MapsPersistentHost`
 * (within `AppShell`) to preserve map state and prevent re-loading Google Maps SDK / tile API
 * quota on every page visit.
 */
export default function MapsPage() {
  return null;
}
